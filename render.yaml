services:
  - type: web
    name: bfhl-api
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OFFICIAL_EMAIL
        sync: false
      - key: GEMINI_API_KEY
        sync: false
